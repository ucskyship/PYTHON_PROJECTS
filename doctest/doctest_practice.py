# import doctest


def add(a, b):
    """
    >>> add(3, 5)
    8
    """
    return a + b

# you can write doctests for your functions and confirm the test using the terminal. First in the terminal,
# run: the following command - python3.10 -m doctest (then (fileName)). Example = -m doctest doctest_practice.py
